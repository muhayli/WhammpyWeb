@page
@model WhammyWeb.Pages.Admin.FoodTypes.IndexModel
@{
    ViewData["Title"] = "Food Types";
}

@*<partial name="_Notification" />*@

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Food Types List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-page="create" class="btn btn-primary">
                <i class="bi bi-plus-square"></i>
                &nbsp;
                Create New Food type
            </a>
        </div>
    </div>

    <br />
    <br />

    <table class="table table-bordered table-striped">
        <thead>
            <tr>

                <th>
                    Food type Name
                </th>
                <th>Action</th>

            </tr>
        </thead>
        <tbody>
            @foreach (var ft in Model.FoodTypes)
            {
                <tr>
                    <td>@ft.Name</td>
                    <td>
                        <div class="btn-group" role="group">
                            @*passing params to other page/screen*@
                            <a asp-page="Edit" asp-route-id="@ft.Id" class="btn btn-success mx-2">
                                <i class="bi bi-pencil-square"></i>
                            </a>
                            <a asp-page="Delete" asp-route-id="@ft.Id" class="btn btn-danger mx-2">
                                <i class="bi bi-trash-fill"></i>
                            </a>
                        </div>
                    </td>
                </tr>
            }


        </tbody>
    </table>
</div>